# 验收记录

日期：2026-09-16。

## 交付清单

- 七个编号板块，每个板块有正文.md和配图/；第05板块另有案例/活动反馈.csv。第02—04板块有独立来源.md。
- 共七篇完整正文，18处图文配合：13张新增解释图、5张沿用并注明来源的mini-llm图片。新增图各有自包含HTML与SVG。
- 课程级README、AGENTS、CONFIG、讨论决策和来源记录已保存。网页源码、锁文件、初始化、构建、校验、预览、Dockerfile、Compose及配置示例齐备。
- 已按用户后续要求取消正文和图片集中存放的方式；网页与图解构建脚本读取各板块目录。

## 本次实测

| 检查 | 结果 |
| --- | --- |
| pnpm build | 生成7个独立文章页、默认入口、导航及静态资源 |
| pnpm check | 7篇正文SHA256与构建对应，内部页面、图片、案例下载、标题锚点、唯一ID、当前页和前后篇导航检查通过 |
| Markdown相对链接 | 七篇全部本地链接指向存在文件 |
| 案例核算 | 12个唯一ID，原文与文章表格逐条一致；声音问题F01/F03共2条，练习节奏5条，全分类覆盖12条 |
| 原图复制 | 5张mini-llm源图与副本SHA256一致，记录见第02板块来源 |
| 图解Skill检查 | 13个HTML均通过self_check.py；13个SVG通过XML解析，title/desc按文件名前缀命名 |
| 图形静态结构 | 修订4px坐标/字体取整、轴标签、文本换行、活动网络端点；文字近似宽度检查无画布越界 |
| HTTP读取 | 41次页面/资源请求通过（含首页重复检查），坏编码返回400、不存在文件404、路径越界403，错误请求后服务仍可访问 |
| Docker Compose | config解析通过；Docker daemon不可连接，未运行容器构建和启动验收 |

## 验收边界

本地预览入口为 http://localhost:4173/ 。无需登录，无后端，不发布公网。

未获当前任务图形浏览器自动化授权，未进行桌面/手机截图、字体实际渲染和交互点击验收。响应式布局、折叠目录、当前小节高亮、图片键盘与点击放大均已实现并作代码检查，但不将其记录为视觉实测。Skill安装目录未提供verify-geometry.py；自检、XML和近似文字边界检查不能保证所有设备无视觉遮挡。

原视频仅作记录阅读，没有重跑转写、读取私人录音或重新验收旧视频。输入输出视频的静音预览与后续有声制作在第07篇明确区分。

## 第四章八项正文续写（2026-09-16）

- 完成八项顺序编排并通读全文，新增三节及三张原文配图。
- 逐行确认原有五组 A/B 对话、十张 GIF 引用保持不变；本轮未修改 GIF 文件与导出程序。
- 修正概念解释，补充来源、教学示例与实测的边界。
- pnpm build、pnpm check 通过，七篇 Markdown 与 HTML 一致，页面、锚点、图片和内部链接有效。
- 本地预览原先未运行，已启动；HTTP 读取第四章并检查八项内容通过。
- 本轮未执行浏览器视觉检查，新增图片的实际屏幕观感未验收。

## 第四章纵向图文与补充 GIF（2026-09-17）

- 八模块按确认顺序重写为 GIF、对应文字、GIF、对应文字，共 16 个三级段落、16 张 GIF；不再使用双栏案例表格。
- 原文正文及配图文字已对照阅读，先推理后结论核对用户截图；原文摘录与课程补充分开标记。
- 原有十张 GIF SHA-256 与本次修改前一致。
- 新增隐式提纯、先推理后结论、语义退火各两张 GIF。ffprobe 确认均为 2040×1140、26 帧、1.62 秒。
- 导出器逐帧检查标签完成绘制，六张导出全部通过；查看三个新增模块 B 路径中间关键帧，标签可读。未进行完整动画视觉播放或浏览器桌面/手机布局检查，不将关键帧检查视作全部视觉验收。
- pnpm build、pnpm check 通过；七篇正文、网页、锚点、图片和内部链接检查通过。
- HTTP 读取本地第四章通过，生成页面包含 16 张 GIF。预览入口仍为 http://localhost:4173/04-表达需求与反馈.html 。

## 第四章对话标识（2026-09-17）

- 按用户要求为人的输入添加 > 标识，为 AI 回复添加红色 ●；共 15 处输入、16 处回复起点。作者评语与工具记录不冒充对话角色。
- 关键提示词、偏离节点与原文评语以红色强调。对话引用区使用白底，规则仅匹配带有专用标记的内容。
- 去除新增内联标记后与修改前正文逐字一致，GIF 引用不变。
- pnpm build、pnpm check 通过。未运行浏览器视觉检查。

## AI 对话底色（2026-09-17）

AI 回复按完整对话段落设置浅灰底，人的输入保持白底；去掉原文摘录、原文评语等标签。构建与链接检查通过，生成 HTML 检查确认 16 个 AI 回复标识均位于 AI 底色容器，人的输入单独分组，16 张 GIF 引用保持。未运行浏览器视觉检查。

## 对话合并底框（2026-09-17）

按用户截图反馈，将同一案例下连续对话合并为一个浅灰底框，不按角色拆卡片。12 个对话组 HTML 结构检查通过，标题与 GIF 均在框外。构建与资源检查通过；未运行浏览器视觉检查。

## 阅读布局与双侧收起（2026-09-17）

- 根据四条页面批注，收窄左侧目录、缩减栏间留白、放宽正文，左右侧栏支持独立朝外侧收起。
- 收起后对应网格列缩为 2rem，保留可恢复按钮；正文占用剩余空间。移动端原有菜单与本篇目录保持。
- 构建与资源检查通过。Node VM 交互检查通过：左右独立收起/展开、刷新状态恢复、aria-expanded 同步。
- 未启动浏览器视觉自动化，本轮不宣称已完成桌面与移动端视觉验收。

## GIF 黑边收紧（2026-09-17）

16 张 GIF 按同组 A/B 全帧联合边界显示，保留 24px 安全边距，最大显示宽度 40rem。416 帧非黑像素边界全部在显示范围内；生成 HTML 含 16 个裁边容器。构建与链接检查通过。原文件不改，未运行浏览器视觉检查。

## 文档阅读界面完整化（2026-09-17）

- 读取 ce101 页面及 HTML 类名，结合用户提供规格实施；字体最终以用户确认的无衬线方案为准。
- 七页均包含跳到正文、机器索引、顶部搜索、品牌图标、面包屑、更多操作及 Copy page 菜单。标题锚点保留且增加复制入口。
- 首篇末尾提供六张课程导航卡片；全部页面使用卡片翻页与本课程页脚。
- 构建和资源检查通过。七篇/57个章节检索验证通过，中文、英文、多关键词、空查询与无结果均已检查；所有搜索结果锚点可在对应 HTML 中找到。
- 检查三种复制入口、下载 Markdown 资源、llms.txt 七篇路径、标题复制控件数量及语义骨架；JS 语法检查通过。搜索 JS 通过 HTTP 返回 JavaScript MIME。
- 浏览器实际剪贴板授权、菜单键盘交互、桌面与手机视觉效果未运行图形浏览器验收，不将静态结构检查等同于完整浏览器实测。

## 2026-09-17：按 CE101 实测值快速对齐

- 以 `网页/设计研究/ce101/参数实测.md` 为依据，替换叠加的页面样式；未修改七篇 Markdown 或 GIF 文件。
- 1440px 浏览器实测：正文 x=363.195、宽688.805；搜索 x=501.328、宽437.336、高36；左栏288、右栏304。与参考站对应尺寸一致。
- 1170px：正文宽770.805、右栏隐藏；390px：正文 x20、宽350，H1 30/36，复制操作在导语下方。三档均无横向溢出。
- 已查看桌面、手机截图；手机抽屉过渡结束后正确收起。左右把手可独立收起并恢复，正文随之扩宽。
- 浏览器点击 Copy page 显示“已复制”；搜索“语义漂移”返回3个相关章节。已恢复目录展开状态及默认视口。
- `pnpm run build`、`pnpm run check` 通过：7篇正文与页面一致，锚点、图片和内部链接有效。
