# 第 04 章原表达保留与 GIF 修复

2026-09-16。用户要求保留原 GIF 与文字表达方式。

- 正文及来源已从优化前备份恢复，字节比对一致。五组原案例、原措辞、双栏对照均保留。
- 活动反馈重写、白底固定字幕、10 秒动画版本不采用，已移入项目根目录 `.trash/Course/AI机制与协作/04未采用改写-20260916/`。
- 现用 GIF 保留原场景、原标签、原字体、原相机和黑底风格；26 帧、1,620 ms，与旧版节奏一致。
- 仅修复：每帧清底以消除残影；增加原样式标签的避让位置，拥挤时寻找最近空位；输出从 1020×570 提高至 2040×1140，调色板由 128 色提高至 256 色。
- 使用原沙盘的纯 Canvas 绘制函数离线导出。没有修改其他案例模块的内容。
- 10 张 GIF 均已解码核对尺寸、帧数、时长。导出过程逐帧检查当前应显示的标签均被绘制；末尾密集标签已抽看，无文字重叠。
- `pnpm build`、`pnpm check` 通过。当前网页重新使用原正文与双栏结构。
- 这次修复不表示原文技术断言已核验，也不表示双栏中的小字或快速节奏适合所有阅读场景；这些涉及表达方式，未进一步调整。
