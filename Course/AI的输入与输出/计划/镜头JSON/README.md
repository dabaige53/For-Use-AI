# 镜头提示词

一个镜头一个 JSON，共八份。每份的 `prompt` 是可单独复制的完整生图提示词，已经包含本镜布局、造型、逐格内容、准确文字与约束，不需要另拼通用提示词。`voiceover_not_for_image` 仅供配音参考，不送入图中文字。

按 S01—S08 顺序使用；每份生成一张同一镜头的演进分镜板。使用参考图片时只能辅助保持已定义的造型与位置，不能继承旧失败图的布局。当前仅整理提示词，未生图验证。
