# 本轮设计依据

核验日期：2026-09-16。正文版本 R1，镜头提示词版本 R2。

| 断言或设计 | 一手依据 | 本轮范围与边界 |
|---|---|---|
| 输入可含系统指令、工具、外部信息；上下文需要选择和维护 | [Anthropic 上下文工程](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents) | 重新读取上下文构成、动态检索和压缩相关正文；不采用性能数字或物理类比 |
| 消息来源和边界需要保留 | [Hugging Face Chat templates](https://huggingface.co/docs/transformers/en/chat_templating) | 重新查阅模板与角色说明；画面分组是教学图形，不是闭源产品的内部序列截图 |
| 模型请求、客户端执行、结果回传后继续调用 | [Claude 工具流程](https://platform.claude.com/docs/en/agents-and-tools/tool-use/how-tool-use-works) | 查阅工具循环说明；课程采用宿主连接外部工具的路径，不泛化为所有工具都在本地执行 |
| inputSchema、tools/call 与响应匹配 | [MCP 2025-06-18 Tools](https://modelcontextprotocol.io/specification/2025-06-18/server/tools) | 读取定义、调用示例和错误部分；固定规范版本，不声称最新版本；MCP id 与模型端调用关联分层处理 |
| transcribe_audio 的参数字段、默认值与调用分派 | [固定提交 index.ts](https://github.com/jwulff/whisper-mcp/blob/ad0199987512700132cc19155a430cc5b76ebace/src/index.ts) | 本地快照与远端原文件逐字节一致；读取注册和处理器，未读 transcribe 实现、未安装或运行 |

源码 SHA256：`3fc12ac09f331b17bf7422e237b296673b54bdf2d600270181e69c68f94f9502`。本地 [完整 Schema](来源/whisper-mcp/transcribe_audio.schema.json) 保留 `name`、`description`、`inputSchema`；四个属性为 `file_path`、`model`、`language`、`output_format`，`required` 仅 `file_path`。处理器中默认 `model=base.en`、`language=en`；本课假设中文录音，显式选用 `large` 与 `zh`，不依赖默认英文配置。参数合法性与成功转写不是同一个结论。

所有系统指令、文件路径、调用值、录音、成功结果和最终纪要均为教学假设。未读取真实文件，未执行转录工具。结果的时间刻度是示意，不承诺该实现的返回 JSON 结构；未把请求与结果图形的配对标记伪装为 Schema 字段。

视觉直接查看了本地 append.jpg、description.jpg、user.jpg，参考其黑底、来源色与关系布局。查看了交接失败图，确认其主要由文字行和 JSON 特写组成。八段、约 180 秒、对象坐标及每镜四格或六格关键状态均为本课的设计判断，尚无实际画面或时长测量。

此前资料索引见[来源清单](来源清单.md)，其阅读范围保留原日期；当前采用的事实范围以本文件和正文引用为准。
